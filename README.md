ConsoleC is a Windows console overlay, built for developers, that appears on the press of a button.

It has over 25 commands, and adding more is as simple as dragging and dropping.
It's currently a work in progress, but is on its way to being a finished product.
